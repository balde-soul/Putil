# coding=utf-8

'''
the PR curve has already prorived in scikit-learn

a example calculating PR curve as below:

    precision, recall, _thresholds = metrics.precision_recall_curve(label, prob) # the P R and the corresponding threshold
    area = metrics.auc(recall, precision) # the AP value
'''
